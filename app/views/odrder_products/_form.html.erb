<%= form_for(odrder_product) do |f| %>
  <% if odrder_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(odrder_product.errors.count, "error") %> prohibited this odrder_product from being saved:</h2>

      <ul>
      <% odrder_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :order_id %>
    <%= f.text_field :order_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
